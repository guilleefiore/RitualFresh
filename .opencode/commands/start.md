---
description: Levanta PostgreSQL y backend con Docker, y frontend con Vite.
agent: build
---

Ejecuta el arranque local completo de RitualFresh: PostgreSQL y backend con Docker Compose, frontend con Vite/npm.

Corre exactamente este comando desde la raiz del proyecto:

```bash
./scripts/start-dev.sh
```

Luego informa al usuario el resultado, incluyendo las URLs locales y las rutas de logs que muestre el script.
