// Minimal auth module page. Replace this with real login/register/restore flows later.
export function AuthPage() {
  return (
    <main className="screen">
      <section className="card">
        <p className="eyebrow">M01 Auth</p>
        <h1>Gestión de usuarios y autenticación</h1>
        <p className="muted">
          Esta carpeta queda lista para login, registro, validación y recuperación de contraseña.
        </p>
      </section>
    </main>
  );
}
